<h1> Editting post <%= @post.id %> </h1>
<%= form_for @post do |f| %>
	<p>
		<%= f.label :title %> <br />
		<%= f.text_field :title %> <br />
		<%= f.label :content %> <br />
		<%= f.text_area :content %> <br />
		
		<%= f.submit "Update the post" %>
	</p>
<% end %>
